## API Report File for "@doccop/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { Document } from '@xmldom/xmldom';
import type { Element } from '@xmldom/xmldom';

// @public
export class AbsentValueInStrictModeError extends DocCopError {
    constructor(tag: string, reason: string | undefined);
    // (undocumented)
    readonly code: "ABSENT_VALUE_IN_STRICT_MODE";
}

// @public
export interface AllocateContext {
    parties: PartyRef[];
    templateCategory: string | null;
    // (undocumented)
    userId: UserId;
    year: number;
}

// @public
export interface AnchorMap {
    blockSdts: Array<{
        tag: string;
        alias: string;
        paraIds: string[];
    }>;
    paragraphs: AnchorParagraph[];
}

// @public
export interface AnchorParagraph {
    // (undocumented)
    paraId: string;
    sdts: AnchorSdt[];
}

// @public
export interface AnchorSdt {
    alias: string;
    block: boolean;
    indexInPara: number | null;
    tag: string;
}

// @public
export interface AuthAdapter {
    // (undocumented)
    canEditTemplate(userId: UserId, templateId: string): Promise<boolean>;
    // (undocumented)
    canManageSnippets(userId: UserId): Promise<boolean>;
    // (undocumented)
    canRenderTemplate(userId: UserId, templateId: string): Promise<boolean>;
    userIdFromRequest(req: unknown): Promise<UserId | null>;
}

// @public
export class AuthForbiddenError extends DocCopError {
    constructor(action: string);
    // (undocumented)
    readonly code: "AUTH_FORBIDDEN";
}

// @public
export const BARE_KEY_PATTERN: RegExp;

// @public
export interface BareKeyPlaceholderSpec {
    alias: string;
    dataType: DataType;
    key: string;
}

// @public
export interface BlockWrapLocation {
    endParaId: string;
    startParaId: string;
}

// @public
export interface CompiledToken {
    key: string;
    paragraphIndex: number;
    paraId: string;
}

// @public
export function compileTextTokens(archive: DocxArchive, options?: CompileTextTokensOptions): CompileTextTokensResult;

// @public
export interface CompileTextTokensOptions {
    delimiters?: {
        open: string;
        close: string;
    };
    onUnknownKey?: "ignore" | "warn" | "error";
    validateKey?: (key: string) => boolean;
}

// @public
export interface CompileTextTokensResult {
    archive: DocxArchive;
    compiled: CompiledToken[];
    skipped: SkippedToken[];
}

// @public
export type DataType = "text" | "number" | "integer" | "date" | "boolean" | "edrpou" | "rnokpp" | "iban" | "email" | "phone";

// @public
export type DecomposedTag = {
    kind: "value";
    tag: string;
    scope: VariableScope;
    key: string;
} | {
    kind: "requisites";
    tag: string;
    partyRole: string;
};

// @public
export function decomposeTag(tag: string): DecomposedTag;

// @public
export interface DocCopConfig {
    // (undocumented)
    auth?: AuthAdapter;
    // (undocumented)
    limits?: Partial<EngineLimits>;
    logger?: Logger;
    // (undocumented)
    naming: NamingService;
    // (undocumented)
    numbering: NumberingService;
    // (undocumented)
    render?: Partial<RenderOptions>;
    // (undocumented)
    requisitesResolver: RequisitesResolver;
    // (undocumented)
    resolvers: EntityResolver[];
    // (undocumented)
    storage: StorageAdapter;
}

// @public
export class DocCopError extends Error {
    constructor(code: DocCopErrorCode, message: string, details?: Record<string, unknown>);
    // (undocumented)
    readonly code: DocCopErrorCode;
    // (undocumented)
    readonly details: Readonly<Record<string, unknown>>;
}

// @public
export type DocCopErrorCode = "MALFORMED_DOCX" | "TEMPLATE_TOO_LARGE" | "SNIPPET_TOO_LARGE" | "TOO_MANY_PLACEHOLDERS" | "XXE_DETECTED" | "INVALID_PLACEHOLDER_TAG" | "OVERLAPPING_PLACEHOLDER" | "PLACEHOLDER_NOT_FOUND" | "SNIPPET_CANNOT_CONTAIN_REQUISITES" | "NO_RESOLVER_FOR_SCOPE" | "RESOLVER_FAILED" | "ABSENT_VALUE_IN_STRICT_MODE" | "TYPE_VALIDATION_FAILED" | "RENDER_TIMEOUT" | "STORAGE_FAILED" | "AUTH_FORBIDDEN" | "VERSION_CONFLICT" | "IDEMPOTENCY_CONFLICT" | "INTERNAL_ERROR";

// @public
export interface DocxArchive {
    document: Document;
    rawParts: Map<string, Uint8Array>;
}

// @public
export interface EngineLimits {
    maxPlaceholders: number;
    maxSnippetBytes: number;
    maxTemplateBytes: number;
    renderTimeoutMs: number;
}

// @public
export function ensureNamespaceOnRoot(doc: Document, prefix: string, nsUri: string): void;

// @public
export function ensureParaIds(archive: DocxArchive): EnsureParaIdsResult;

// @public
export interface EnsureParaIdsResult {
    generated: number;
    preserved: number;
}

// @public
export type EntityId = string;

// @public
export interface EntityResolver {
    resolve(key: string, ctx: ResolveContext): Promise<ResolvedValue>;
    readonly scope: VariableScope;
}

// @public
export type EntitySubtype = string;

// @public
export type EntityType = string;

// @public
export function findByParaId(archive: DocxArchive, paraId: string): Element | null;

// @public
export function findElements(doc: Document | Element, nsUri: string, localName: string): Element[];

// @public
export class IdempotencyConflictError extends DocCopError {
    constructor(key: string);
    // (undocumented)
    readonly code: "IDEMPOTENCY_CONFLICT";
}

// @public
export interface InjectionRequest {
    dataTypes?: ReadonlyMap<string, DataType>;
    master: DocxArchive;
    renderConfig: RenderConfig;
    renderRequest: RenderRequest;
    snippet: TemplateSnippet;
    stylePrefix: string;
    tag: string;
    targetParty: string;
}

// @public
export function injectRequisites(req: InjectionRequest): Promise<DocxArchive>;

// @public
export class InternalError extends DocCopError {
    constructor(detail: string, cause?: unknown);
    // (undocumented)
    readonly code: "INTERNAL_ERROR";
}

// @public
export class InvalidPlaceholderTagError extends DocCopError {
    constructor(tag: string, reason: string);
    // (undocumented)
    readonly code: "INVALID_PLACEHOLDER_TAG";
}

// @public
export function list(archive: DocxArchive, dataTypes?: ReadonlyMap<string, DataType>, options?: ListOptions): Placeholder[];

// @public
export interface ListOptions {
    includeBareKey?: boolean;
}

// @public
export function listParagraphs(archive: DocxArchive): Array<{
    paraId: string;
    element: Element;
}>;

// @public
export interface Logger {
    // (undocumented)
    debug(message: string, fields?: Record<string, unknown>): void;
    // (undocumented)
    error(message: string, fields?: Record<string, unknown>): void;
    // (undocumented)
    info(message: string, fields?: Record<string, unknown>): void;
    // (undocumented)
    trace(message: string, fields?: Record<string, unknown>): void;
    // (undocumented)
    warn(message: string, fields?: Record<string, unknown>): void;
}

// @public
export class MalformedDocxError extends DocCopError {
    constructor(detail: string, cause?: unknown);
    // (undocumented)
    readonly code: "MALFORMED_DOCX";
}

// @public
export interface NamingContext {
    now: Date;
    number: string;
    parties: PartyRef[];
    // (undocumented)
    templateCategory: string | null;
    templateName: string;
}

// @public
export interface NamingService {
    // (undocumented)
    format(ctx: NamingContext): string;
}

// @public
export const NoopLogger: Logger;

// @public
export class NoResolverForScopeError extends DocCopError {
    constructor(scope: string, tag: string);
    // (undocumented)
    readonly code: "NO_RESOLVER_FOR_SCOPE";
}

// @public
export interface NumberingService {
    // (undocumented)
    allocate(ctx: AllocateContext): Promise<string>;
}

// @public
export const OOXML_NAMESPACES: Readonly<Record<string, string>>;

// @public
export class OverlappingPlaceholderError extends DocCopError {
    constructor(existingTag: string);
    // (undocumented)
    readonly code: "OVERLAPPING_PLACEHOLDER";
}

// @public
export function parse(bytes: Uint8Array, opts?: ParseOptions): DocxArchive;

// @public
export interface ParseOptions {
    maxBytes?: number;
}

// @public
export function parseXmlSafely(xml: string, partName: string): Document;

// @public
export interface PartyRef {
    entityId: EntityId;
    entityType: EntityType;
    role: string;
}

// @public
export interface Placeholder {
    alias: string;
    dataType: DataType;
    // (undocumented)
    key: string;
    paraId: string;
    scope: VariableScope;
    tag: string;
}

// @public
export class PlaceholderNotFoundError extends DocCopError {
    constructor(tag: string);
    // (undocumented)
    readonly code: "PLACEHOLDER_NOT_FOUND";
}

// @public
export interface PlaceholderSpec {
    alias: string;
    dataType: DataType;
    tag: string;
}

// @public
export function preview(archive: DocxArchive): RenderedHtml;

// @public
export function render(template: DocxArchive, req: RenderRequest, config: RenderConfig, dataTypes?: ReadonlyMap<string, DataType>): Promise<RenderResult>;

// @public
export interface RenderConfig {
    options?: Partial<RenderOptions>;
    requisitesResolver?: RequisitesResolver;
    resolvers: EntityResolver[];
}

// @public
export interface RenderedHtml {
    anchors: AnchorMap;
    html: string;
}

// @public
export interface RenderOptions {
    strict: boolean;
}

// @public
export interface RenderRequest {
    documentNumber: string;
    now?: Date;
    parties: PartyRef[];
    // (undocumented)
    templateCategory: string | null;
    // (undocumented)
    templateId: string;
    // (undocumented)
    templateVersionId: string;
    // (undocumented)
    userId: UserId;
}

// @public
export interface RenderResult {
    docx: Uint8Array;
    durationMs: number;
    resolvedValues: Record<string, string>;
    warnings: RenderWarning[];
}

// @public
export class RenderTimeoutError extends DocCopError {
    constructor(elapsedMs: number, limitMs: number);
    // (undocumented)
    readonly code: "RENDER_TIMEOUT";
}

// @public
export interface RenderWarning {
    // (undocumented)
    detail: string;
    // (undocumented)
    kind: "missing_resolver" | "absent_value" | "type_mismatch" | "snippet_missing";
    // (undocumented)
    tag: string;
}

// @public
export function replace(archive: DocxArchive, tag: string, value: string): DocxArchive;

// @public
export interface RequisitesResolver {
    // (undocumented)
    resolveSnippet(entityType: EntityType, entitySubtype: EntitySubtype): Promise<TemplateSnippet | null>;
}

// @public
export function resolveAndInject(opts: {
    master: DocxArchive;
    tag: string;
    targetParty: string;
    entityType: string;
    entitySubtype: string | null;
    resolver: RequisitesResolver | undefined;
    renderRequest: RenderRequest;
    renderConfig: RenderConfig;
    dataTypes?: ReadonlyMap<string, DataType>;
    stylePrefix: string;
    strict: boolean;
}): Promise<{
    master: DocxArchive;
    skipped: boolean;
    reason?: string;
}>;

// @public
export interface ResolveContext {
    meta: {
        templateId: string;
        templateVersionId: string;
        templateCategory: string | null;
        documentNumber: string | null;
        now: Date;
    };
    parties: Record<string, PartyRef | undefined>;
    userId: UserId;
}

// @public
export type ResolvedValue = {
    kind: "text";
    value: string;
} | {
    kind: "image";
    bytes: Uint8Array;
    mimeType: "image/png" | "image/jpeg";
    widthPx?: number;
    heightPx?: number;
} | {
    kind: "absent";
    reason?: string;
};

// @public
export class ResolverFailedError extends DocCopError {
    constructor(tag: string, cause: unknown);
    // (undocumented)
    readonly code: "RESOLVER_FAILED";
}

// @public
export interface SaveDocumentHint {
    // (undocumented)
    contentType: "application/vnd.openxmlformats-officedocument.wordprocessingml.document";
    // (undocumented)
    ownerId: UserId;
    suggestedName?: string;
}

// @public
export function serialize(archive: DocxArchive): Uint8Array;

// @public
export function serializeArchive(archive: DocxArchive): Uint8Array;

// @public
export function serializeXml(doc: Document): string;

// @public
export interface SkippedToken {
    raw: string;
    reason: string;
}

// @public
export class SnippetCannotContainRequisitesError extends DocCopError {
    constructor(foundTag: string);
    // (undocumented)
    readonly code: "SNIPPET_CANNOT_CONTAIN_REQUISITES";
}

// @public
export class SnippetTooLargeError extends DocCopError {
    constructor(actualBytes: number, maxBytes: number);
    // (undocumented)
    readonly code: "SNIPPET_TOO_LARGE";
}

// @public
export interface StorageAdapter {
    loadDocument(path: string): Promise<Uint8Array>;
    loadSnippet(path: string): Promise<Uint8Array>;
    loadTemplate(path: string): Promise<Uint8Array>;
    saveDocument(bytes: Uint8Array, hint: SaveDocumentHint): Promise<string>;
    saveSnippet(bytes: Uint8Array, ownerId: UserId): Promise<string>;
    saveTemplate(bytes: Uint8Array, ownerId: UserId): Promise<string>;
    signedUrl(path: string, expiresInSec: number): Promise<string>;
}

// @public
export class StorageFailedError extends DocCopError {
    constructor(operation: string, cause: unknown);
    // (undocumented)
    readonly code: "STORAGE_FAILED";
}

// @public
export interface TemplateSnippet {
    bytes: Uint8Array;
    // (undocumented)
    entitySubtype: EntitySubtype;
    // (undocumented)
    entityType: EntityType;
    // (undocumented)
    id: string;
    placeholders: Placeholder[];
}

// @public
export class TemplateTooLargeError extends DocCopError {
    constructor(actualBytes: number, maxBytes: number);
    // (undocumented)
    readonly code: "TEMPLATE_TOO_LARGE";
}

// @public
export class TooManyPlaceholdersError extends DocCopError {
    constructor(actualCount: number, maxCount: number);
    // (undocumented)
    readonly code: "TOO_MANY_PLACEHOLDERS";
}

// @public
export class TypeValidationFailedError extends DocCopError {
    constructor(tag: string, expected: string, actual: string);
    // (undocumented)
    readonly code: "TYPE_VALIDATION_FAILED";
}

// @public
export function unwrap(archive: DocxArchive, tag: string): DocxArchive;

// @public
export type UserId = string;

// @public
export function validateAlias(tag: string, alias: string): void;

// @public
export function validateBareKey(key: string): void;

// @public
export function validateValue(dataType: DataType, tag: string, value: string): string;

// @public
export type VariableScope = `party_${string}` | "system" | "custom" | "bareKey";

// @public
export class VersionConflictError extends DocCopError {
    constructor(templateId: string, expected: string, actual: string);
    // (undocumented)
    readonly code: "VERSION_CONFLICT";
}

// @public
export const W14_NS = "http://schemas.microsoft.com/office/word/2010/wordml";

// @public
export const W_NS = "http://schemas.openxmlformats.org/wordprocessingml/2006/main";

// @public
export function wrap(archive: DocxArchive, loc: WrapLocation, spec: PlaceholderSpec): DocxArchive;

// @public
export function wrapBareKey(archive: DocxArchive, loc: WrapLocation, spec: BareKeyPlaceholderSpec): DocxArchive;

// @public
export function wrapBlock(archive: DocxArchive, opts: BlockWrapLocation, spec: PlaceholderSpec): DocxArchive;

// @public
export interface WrapLocation {
    endOffset: number;
    endRunIndex: number;
    paraId: string;
    startOffset: number;
    startRunIndex: number;
}

// @public
export class XxeDetectedError extends DocCopError {
    constructor(entityName: string);
    // (undocumented)
    readonly code: "XXE_DETECTED";
}

// (No @packageDocumentation comment for this package)

```
