## API Report File for "@doccop/server"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { AuthAdapter } from '@doccop/core';
import type { DataType } from '@doccop/core';
import type { EntityResolver } from '@doccop/core';
import type { FastifyPluginAsync } from 'fastify';
import type { NamingService } from '@doccop/core';
import type { NumberingService } from '@doccop/core';
import type { Placeholder } from '@doccop/core';
import type { RequisitesResolver } from '@doccop/core';
import type { StorageAdapter } from '@doccop/core';
import type { UserId } from '@doccop/core';

// @public
export interface DataTypeRegistry {
    // (undocumented)
    forTag(tag: string): Promise<DataType | null>;
    snapshot(): Promise<ReadonlyMap<string, DataType>>;
}

// Warning: (ae-forgotten-export) The symbol "PluginOptions" needs to be exported by the entry point index.d.ts
//
// @public
export const doccopRoutes: FastifyPluginAsync<PluginOptions>;

// @public (undocumented)
export interface DoccopServerConfig {
    // (undocumented)
    auth: AuthAdapter;
    // (undocumented)
    dataTypes: DataTypeRegistry;
    // (undocumented)
    documents: DocumentStore;
    // (undocumented)
    idempotency: IdempotencyStore;
    maxConcurrentRendersPerUser?: number;
    maxUploadBytes?: number;
    // (undocumented)
    naming: NamingService;
    // (undocumented)
    numbering: NumberingService;
    rateLimitPerMinute?: number;
    // (undocumented)
    requisitesResolver?: RequisitesResolver;
    // (undocumented)
    resolvers: EntityResolver[];
    // (undocumented)
    snippets: SnippetStore;
    // (undocumented)
    snippetVersions: SnippetVersionStore;
    // (undocumented)
    storage: StorageAdapter;
    strictRender?: boolean;
    // (undocumented)
    templates: TemplateStore;
    // (undocumented)
    templateVersions: TemplateVersionStore;
}

// @public (undocumented)
export interface DocumentStore {
    // (undocumented)
    create(record: Omit<GeneratedDocumentRecord, "id" | "createdAt">): Promise<GeneratedDocumentRecord>;
    // (undocumented)
    get(id: string): Promise<GeneratedDocumentRecord | null>;
    listByOwner(userId: UserId): Promise<GeneratedDocumentRecord[]>;
}

// @public
export interface GeneratedDocumentRecord {
    // (undocumented)
    createdAt: string;
    // (undocumented)
    createdBy: UserId;
    // (undocumented)
    id: string;
    // (undocumented)
    name: string;
    // (undocumented)
    number: string;
    // (undocumented)
    parties: Array<{
        role: string;
        entityType: string;
        entityId: string;
    }>;
    // (undocumented)
    sizeBytes: number;
    // (undocumented)
    storagePath: string;
    // (undocumented)
    templateId: string;
    // (undocumented)
    templateVersionId: string;
    // (undocumented)
    variablesSnapshot: Record<string, string>;
}

// @public
export interface IdempotencyStore {
    lookup(key: string, userId: UserId): Promise<string | null>;
    store(key: string, userId: UserId, documentId: string): Promise<void>;
}

// @public (undocumented)
export class InMemoryRateLimiter {
    // Warning: (ae-forgotten-export) The symbol "RateLimiterOptions" needs to be exported by the entry point index.d.ts
    constructor(opts: RateLimiterOptions);
    acquire(userId: UserId, now?: number): null | (() => void);
    // Warning: (ae-forgotten-export) The symbol "UserState" needs to be exported by the entry point index.d.ts
    inspect(userId: UserId): Readonly<UserState> | undefined;
}

// @public
export interface SnippetRecord {
    // (undocumented)
    createdAt: string;
    // (undocumented)
    currentVersionId: string | null;
    // (undocumented)
    entitySubtype: string;
    // (undocumented)
    entityType: string;
    // (undocumented)
    id: string;
    // (undocumented)
    name: string;
    // (undocumented)
    ownerId: UserId;
    // (undocumented)
    updatedAt: string;
}

// @public (undocumented)
export interface SnippetStore {
    // (undocumented)
    delete(id: string): Promise<boolean>;
    // (undocumented)
    findBySubtype(entityType: string, entitySubtype: string): Promise<SnippetRecord | null>;
    // (undocumented)
    get(id: string): Promise<SnippetRecord | null>;
    // (undocumented)
    list(): Promise<SnippetRecord[]>;
    // (undocumented)
    setCurrentVersion(snippetId: string, newVersionId: string): Promise<SnippetRecord | null>;
    // (undocumented)
    upsert(record: Omit<SnippetRecord, "id" | "createdAt" | "updatedAt" | "currentVersionId">): Promise<SnippetRecord>;
}

// @public (undocumented)
export interface SnippetVersionRecord {
    // (undocumented)
    createdAt: string;
    // (undocumented)
    createdBy: UserId;
    // (undocumented)
    id: string;
    // (undocumented)
    placeholders: Placeholder[];
    // (undocumented)
    snippetId: string;
    // (undocumented)
    storagePath: string;
    // (undocumented)
    versionNumber: number;
}

// @public (undocumented)
export interface SnippetVersionStore {
    // (undocumented)
    create(record: Omit<SnippetVersionRecord, "id" | "createdAt">): Promise<SnippetVersionRecord>;
    // (undocumented)
    get(id: string): Promise<SnippetVersionRecord | null>;
}

// @public
export interface TemplateRecord {
    // (undocumented)
    categoryId: string | null;
    // (undocumented)
    createdAt: string;
    // (undocumented)
    currentVersionId: string | null;
    // (undocumented)
    description: string | null;
    // (undocumented)
    id: string;
    // (undocumented)
    name: string;
    // (undocumented)
    ownerId: UserId;
    // (undocumented)
    partyCount: number;
    // (undocumented)
    updatedAt: string;
    // (undocumented)
    visibility: "private" | "global";
}

// @public (undocumented)
export interface TemplateStore {
    // (undocumented)
    create(record: Omit<TemplateRecord, "id" | "createdAt" | "updatedAt" | "currentVersionId">): Promise<TemplateRecord>;
    // (undocumented)
    delete(id: string): Promise<boolean>;
    // (undocumented)
    get(id: string): Promise<TemplateRecord | null>;
    listVisible(userId: UserId): Promise<TemplateRecord[]>;
    setCurrentVersion(templateId: string, expectedPreviousId: string | null, newVersionId: string): Promise<TemplateRecord | null>;
}

// @public
export interface TemplateVersionRecord {
    // (undocumented)
    changeSummary: string | null;
    createdAt: string;
    // (undocumented)
    createdBy: UserId;
    // (undocumented)
    id: string;
    // (undocumented)
    placeholders: Placeholder[];
    // (undocumented)
    storagePath: string;
    // (undocumented)
    templateId: string;
    // (undocumented)
    versionNumber: number;
}

// @public (undocumented)
export interface TemplateVersionStore {
    // (undocumented)
    create(record: Omit<TemplateVersionRecord, "id" | "createdAt">): Promise<TemplateVersionRecord>;
    // (undocumented)
    get(id: string): Promise<TemplateVersionRecord | null>;
    // (undocumented)
    listByTemplate(templateId: string): Promise<TemplateVersionRecord[]>;
}

// @public
export function withDefaults(c: DoccopServerConfig): Required<Omit<DoccopServerConfig, "storage" | "templates" | "templateVersions" | "snippets" | "snippetVersions" | "documents" | "idempotency" | "dataTypes" | "resolvers" | "requisitesResolver" | "numbering" | "naming" | "auth">> & DoccopServerConfig;

// (No @packageDocumentation comment for this package)

```
